import React from 'react'
import {View,Text, Stylesheet, Dimensions, Image} from 'react-native'

import ImageDate from './Components/ImageData' 


const ImageSlider = () => {
    return (
        <div>
            
        </div>
    )
}

export default ImageSlider


//need to import images from another list or a list. 
    // needs to be objects inside an array
        //cycle through the array or map and + 1 to the image so it cycles to next image
            //use state 
                // touchable opacity for the swipe? do I need an Onpress event added to it to update the state?  
// use a flatlist for part of comonent.
// need to update state 